for = (execLambda, conditionLambda) -> {
    if(conditionLambda(), () -> {
        execLambda()
        for(execLambda, conditionLambda)
    })
}

{
    index = 0
    self = this
    until = () -> {
        <(index, 10)
    }
    each = () -> {
        print(index)
        self.index = +(index, 1)
    }
    for(each,until)
}
